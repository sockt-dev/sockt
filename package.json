{
  "name": "sockt",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '@sockt/types' build && bun run --filter '@sockt/fsm' build && bun run --filter '@sockt/memory' build && bun run --filter '@sockt/slack-gateway' build && bun run --filter '@sockt/runtime' build && bun run --filter '@sockt/orch' build && bun run --filter '@sockt/cadvp' build",
    "test": "bun test",
    "lint": "eslint packages/*/src",
    "generate-schemas": "bun run packages/types/scripts/generate-schemas.ts",
    "orch": "bun run packages/orch/src/start.ts",
    "ui": "bun run packages/ui/src/index.ts",
    "ui:dev": "bun run --hot packages/ui/src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "eslint": "^10.7.0",
    "zod": "^4.4.3",
    "zod-to-json-schema": "^3.25.2"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@ai-sdk/amazon-bedrock": "^5.0.17"
  }
}
